{
  "name": "meet-transcript-pipeline",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev:reset": "tsx scripts/dev-reset.ts",
    "dev:import": "tsx scripts/dev-import.ts",
    "dev:status": "tsx scripts/dev-status.ts"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.5.0"
  },
  "packageManager": "npm@10.2.0"
}